#pragma once

namespace VisualsTAB {
    void Render();                  // Local player field KJNHLAD* (int) read / override UI
    void Tick(bool menuVisible);    // Per-frame write when override enabled
}
